import { Tabs } from "expo-router";

export default function layout(){
    return (
        <Tabs
            screenOptions={
                tabBarActiveTintColor: "red",
                    title: "rocky",
                        tabBarIcon: ({})=>(

                        );
}

/>
    );
}
